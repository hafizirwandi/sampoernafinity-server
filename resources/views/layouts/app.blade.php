{{-- Livewire's default page-component layout (config('livewire.component_layout') = 'layouts::app').
     Only used when a component doesn't declare its own #[Layout(...)], e.g. our
     login page which renders a complete <html> document itself. --}}
{{ $slot }}
